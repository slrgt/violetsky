/**
 * Stub for purplesky_wasm when not built (e.g. CI). Replace with real WASM via: npm run build:wasm
 * The app uses JS fallbacks when these exports are missing.
 */
export default function init() {
  return Promise.resolve();
}
